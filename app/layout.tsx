import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Iskandar Rustamov - Portfolio",
  description:
    "Full Stack Developer Portfolio - Modern web applications with React, Node.js and cutting-edge technologies",
  keywords: "developer, portfolio, react, nodejs, fullstack, web development",
  authors: [{ name: "Iskandar Rustamov" }],
  viewport: "width=device-width, initial-scale=1",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>{children}</body>
    </html>
  )
}
